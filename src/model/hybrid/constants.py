img_size = 384

# Must match the `inter_dim` passed to CFANet in model.py — the width of the pooled
# cross-scale-attention feature our head reads.
inter_dim = 256
abs_value_head_scaledown = 4
dropout = 0.1
